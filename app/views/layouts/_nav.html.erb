<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <span class="title is-size-2"> <%= link_to 'Blurb', root_path, class: "navbar-item has-text-primary" %></span>
    </div>
    <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
            <%= link_to "Posts", posts_path, class: "navbar-item" %>
            <% if current_account %>
                <%= link_to "New Post", new_post_path, class: "navbar-item" %>
            <% end %>
        </div>
        <div class="navbar-end">
            <div class="navbar-item">
                <div class="buttons">
                    <% if current_account %>
                        <%= button_to "Log Out", logout_path, method: :delete, class: "button is-primary" %>
                    <% else %>
                        <%= link_to "Sign Up", signup_path, class: "button is-primary" %>
                        <%= link_to "Sign In", signin_path, class: "button is-light" %> 
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</nav>